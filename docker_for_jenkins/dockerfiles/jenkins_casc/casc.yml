jenkins:
  securityRealm:
    local:
      allowsSignup: false
      users:
       - id: ${JENKINS_ADMIN_ID}
         password: ${JENKINS_ADMIN_PASSWD}
  # Setup the nodes for pipelines
  nodes:
  - permanent:
      # Setup the node docker
      labelString: "docker-host linux rocky"
      launcher:
        ssh:
          credentialsId: "docker-host"
          # Using jenkins in docker the node should always be this one
          # Not if there is a remote server with docker
          host: "172.17.0.1"
          port: 22
          sshHostKeyVerificationStrategy: "nonVerifyingKeyVerificationStrategy"
      name: "docker-host"
      remoteFS: "/home/${DOCKER_NODE_USER}/jenkins_dir"
      retentionStrategy: "always"
  # Setup the cloud docker
  clouds:
  - docker:
      containerCap: 3
      dockerApi:
        connectTimeout: 60
        dockerHost:
          uri: "tcp://172.17.0.1:4243"
        readTimeout: 60
      name: "docker"
      templates:
      - connector:
          ssh:
            sshKeyStrategy:
              manuallyConfiguredSSHKey:
                credentialsId: "agent-credentials"
                sshHostKeyVerificationStrategy: "nonVerifyingKeyVerificationStrategy"
        dockerTemplateBase:
          cpuPeriod: 0
          cpuQuota: 0
          image: "rocky-agent:latest"
        labelString: "docker-agent container rocky linux"
        name: "docker-agent"
        pullStrategy: PULL_NEVER
        pullTimeout: 300
        remoteFs: "/home/jenkins"

unclassified:
  # Setup english as language whatever the browser language
  locale:
    ignoreAcceptLanguage: true
    systemLocale: "english"
  location:
    adminAddress: "sofa.kreutz@yahoo.fr"
    url: "http://localhost:8088/"
# Credentials
credentials:
  system:
    domainCredentials:
    - credentials:
      - usernamePassword:
          # Credentials for the node docker
          description: "docker node"
          id: "docker-host"
          password: ${DOCKER_NODE_PASSWD}
          scope: GLOBAL
          username: ${DOCKER_NODE_USER}
      - usernamePassword:
          description: "rocky agent"
          id: "agent-credentials"
          password: "${ROCKY_AGENT_PASSWD}"
          scope: GLOBAL
          username: "${ROCKY_AGENT_USER}"